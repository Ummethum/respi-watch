# Copy this file to ".env" and fill in your real values.
# ".env" itself is gitignored and will never be committed --
# only this template (.env.example) is safe to push to GitHub.

HF_REPO_ID=yourname/respiwatch-data
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
